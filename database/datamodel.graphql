type Post {
  id: ID! @id
  isPublished: Boolean! @default(value: false)
  title: String!
  text: String!
}

type Course {
  id: ID! @id
  published: Boolean! @default(value: false)
  name: String!
  description: String!
}
